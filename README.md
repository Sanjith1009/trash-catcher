# trash-catcher
Camera will see trash flying towards it, send command to motors to adjust the trashcan and catch the trash.
ROS2 based program structure.
Added complexity of using 2D camera rather than having a depth sensor.
Added complexity of using Stanford's remote linux cluster rather than running ubuntu locally (might change depending on latency needs).
